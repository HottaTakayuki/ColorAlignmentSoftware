<Window x:Class="CAS.WindowProgress"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="WindowProgress" Height="167" Width="300" WindowStartupLocation="CenterScreen" Background="#FF202020" Icon="/Resources/CLED_CAS_App_Icon_170327.ico" Topmost="True" Loaded="WindowProgress_Load">
    <Window.Resources>
        <!--プログレスバー追加項目-->
        <SolidColorBrush x:Key="ProgressBar.Background" Color="#FFE6E6E6"/>
        <SolidColorBrush x:Key="ProgressBar.Progress" Color="#FF0663B0"/>
        <LinearGradientBrush x:Key="ProgressBar.Stripe" StartPoint="0,0.5" EndPoint="1,0.5">
            <GradientStop Color="#FF0663B0" Offset="0.0" />
            <GradientStop Color="#FF0663B0" Offset="0.4" />
            <GradientStop Color="#FF97cefb" Offset="0.5" />
            <GradientStop Color="#FF0663B0" Offset="0.6" />
            <GradientStop Color="#FF0663B0" Offset="1.0" />
        </LinearGradientBrush>
        <!-- プログレスバースタイルを定義 -->
        <Style TargetType="ProgressBar" x:Key="RectangleProgressBar">
            <Setter Property="Background" Value="{StaticResource ProgressBar.Background}"/>
            <Setter Property="Foreground" Value="{StaticResource ProgressBar.Progress}"/>
            <Setter Property="Height" Value="20"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ProgressBar}">
                        <Grid x:Name="TemplateRoot">
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CommonStates">
                                    <VisualState x:Name="Determinate">
                                        <Storyboard RepeatBehavior="Forever">
                                            <RectAnimation Storyboard.TargetName="Stripe"
                                                   Storyboard.TargetProperty="(Shape.Fill).(TileBrush.Viewport)"
                                                   Duration="0:0:2"
                                                   From="0,0,200,200" To="200,0,200,200"/>
                                        </Storyboard>
                                    </VisualState>
                                    <VisualState x:Name="Indeterminate"/>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>
                            <Border Background="{TemplateBinding Background}"/>
                            <Rectangle x:Name="PART_Track"/>
                            <Grid x:Name="PART_Indicator"
                          ClipToBounds="true"
                          HorizontalAlignment="Left">
                                <Rectangle x:Name="Indicator" Fill="{TemplateBinding Foreground}"/>
                                <Rectangle x:Name="Stripe">
                                    <Rectangle.Fill>
                                        <DrawingBrush TileMode="Tile" Stretch="Uniform"
                                              Viewport="0,0,200,200" ViewportUnits="Absolute">
                                            <DrawingBrush.Transform>
                                                <TransformGroup>
                                                    <ScaleTransform/>
                                                    <SkewTransform/>
                                                    <RotateTransform/>
                                                    <TranslateTransform/>
                                                </TransformGroup>
                                            </DrawingBrush.Transform>
                                            <DrawingBrush.Drawing>
                                                <GeometryDrawing Brush="{StaticResource ProgressBar.Stripe}">
                                                    <GeometryDrawing.Geometry>
                                                        <PathGeometry>
                                                            <PathGeometry.Figures>
                                                                <PathFigureCollection>
                                                                    <PathFigure StartPoint="0,0">
                                                                        <LineSegment Point="20,0"/>
                                                                        <LineSegment Point="20,20"/>
                                                                        <LineSegment Point="0,20"/>
                                                                    </PathFigure>
                                                                </PathFigureCollection>
                                                            </PathGeometry.Figures>
                                                        </PathGeometry>
                                                    </GeometryDrawing.Geometry>
                                                </GeometryDrawing>
                                            </DrawingBrush.Drawing>
                                        </DrawingBrush>
                                    </Rectangle.Fill>
                                </Rectangle>
                            </Grid>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid Background="#FF202020" Height="135">
        <Label x:Name="lbMessage" Content="Progress Start!" VerticalAlignment="Top" Foreground="White" Margin="10" Height="AUTO"/>
        <Label x:Name="lbElapsedTime" Content="Elapsed Time: " VerticalAlignment="Top" Foreground="White" Margin="11,50,5,0" HorizontalAlignment="Right" Visibility="Collapsed"/>
        <Label x:Name="lbRemainTime" Content="Remaining Time (Estimated): " VerticalAlignment="Bottom" Foreground="White" Margin="11,0,5,45" HorizontalAlignment="Right" Visibility="Collapsed"/>
        <ProgressBar Style="{StaticResource RectangleProgressBar}" x:Name="pbProgressWhole" Margin="10,66,10,10" VerticalAlignment="Bottom" Height="38" Foreground="#FF0663B0" />
    </Grid>
</Window>
