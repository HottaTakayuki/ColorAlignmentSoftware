
# 2. モジュール配置図（モジュールの物理配置設計）

## 2-1. 物理配置図

```mermaid
flowchart LR
	subgraph CAS["CAS.exe"]
		UI["MDL-WORM-001 WormFixUIController"]
		ENG["MDL-WORM-002 WormFixEngine"]
		POS["MDL-WORM-003 WormFixPositioning"]
		WRT["MDL-WORM-004 WormControllerWriteService"]
	end

	subgraph External["外部連携"]
		ACC["AlphaCameraControl"]
		CAM["CameraControl/CameraControllerSharp"]
		CTRL["Controller(SDCP)"]
		FS[("CSV/画像/ログファイル")]
		CV["OpenCvSharp"]
	end

	UI --> ENG
	UI --> POS
	ENG --> CV
	ENG --> ACC
	ENG --> WRT
	ENG --> CTRL
	ACC --> CAM
	POS --> ACC
	POS --> CTRL
	POS --> CV
	ENG --> FS
	WRT --> CTRL
```

## 2-2. 配置一覧

| 配置区分 | 配置先パス/ノード | 配置モジュール | 配置理由 |
|----------|-------------------|----------------|----------|
| 実行モジュール | CAS/Functions/Data.cs | MDL-WORM-001〜004 | WormFixの各種処理（Backup/Restore/Replace/Recovery等）を担当 |
| 外部カメラ連携 | CameraControl.dll | Positioning/WormFixEngine | 撮影・AF・ライブビューのため |
| 外部制御連携 | Controller (SDCP) | ControllerWrite | 補正値設定・Write/Reconfigのため |
| ファイル永続化 | 測定フォルダ/任意CSVパス | WormFixEngine | 測定結果・補正値の保存/読込のため |
